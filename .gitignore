# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*
# Cloudflare local secrets (aggregator API keys) - never commit the real file
.dev.vars
.dev.vars.*
!.dev.vars.example

# deployment and generated files
.vercel
next-env.d.ts
*.tsbuildinfo
/dist/
/.wrangler/
/outputs/
/work/
/tmp/
/local-backups/

# Design exports dropped in the repo root. The useful content is converted and committed
# under docs/design/; the .pdf and .zip are 3 MB of duplicates of it.
/UX Audit.pdf
/UX Audit.dc.html
/UX and UI improvements.zip
