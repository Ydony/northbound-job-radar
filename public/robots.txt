# Private workspace: every page sends noindex through metadata and X-Robots-Tag.
# Permit fetching so search engines can see those directives. Disallow alone
# can leave a URL indexed without its content. Account data requires a session.
User-agent: *
Disallow:
